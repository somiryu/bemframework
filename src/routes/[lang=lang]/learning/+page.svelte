<script lang="ts">
	// This route always redirects to /academy/manifesto — see +page.server.ts.
	// The component itself never renders, but SvelteKit still needs a +page.svelte
	// present for the route to be valid.
</script>
